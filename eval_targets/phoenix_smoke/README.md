# phoenix_smoke (target_type=smoke) — integration/scale, 효과입증 아님
용도: 대형 실제 프로젝트에서 pipeline 무결성, Strawberry/Starlette/FastAPI source modeling 검증,
AI attribution/scope selection 검증. **phoenix 에서 finding 이 없다고 도구가 효과 없다고 판단하지 않는다.**
phoenix 는 TS/TSX 다수 + Python-only MVP scope + 알려진 CVE 없음 → 효과 입증 타겟으로 부적합.
clone: git clone https://github.com/Arize-ai/phoenix (이 디렉터리엔 메타만, 실제 clone 은 런타임).
